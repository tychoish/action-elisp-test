#+TITLE: elisp-ci: Unified GitHub Action for Emacs Lisp CI
#+AUTHOR: sam kleinman
#+EMAIL: sam@tychoish.com
#+LANGUAGE: en
#+OPTIONS: toc:2 num:nil author:nil email:nil
A streamlined suite of composite GitHub Actions to execute ERT test suites,
configurable linters, and code coverage analysis for Emacs Lisp packages with
zero configuration boilerplate.

* Overview
:PROPERTIES:
:CUSTOM_ID: overview
:END:

=elisp-ci= eliminates repetitive shell boilerplate from Emacs package CI
workflows by encapsulating:

- *Automatic Dependency Discovery*: Automatically scans =Package-Requires=
  headers across package =*.el= source files and installs required ELPA
  dependencies before linting, testing, and coverage measurement. No manual
  dependency lists required in standard setups.

- *Single-Location Dependency & Option Configuration*: If extra dependencies or
  custom paths are needed, they can be declared once at the workflow or job level
  using environment variables (=ELISP_CI_DEPENDENCIES=, =ELISP_CI_LOAD_PATHS=,
  =ELISP_CI_ARCHIVES=) instead of duplicating them across every step.

- *Automatic GPG Keyring & Archive Setup*: Preconfigures GNU ELPA, NonGNU ELPA,
  MELPA, and ELPAish snapshot archives. Automatically fetches and imports the
  ELPAish GPG public keyring for cryptographic signature validation, with
  seamless unsigned archive fallback.

- *Configurable Quality Linters*: Byte-compilation (with warnings-as-errors),
  =checkdoc= docstring linting, =package-lint=, and =relint= with granular
  required/advisory failure gates.

- *Coverage Reporting & UI Summary*: Measures code coverage with =under-cover=
  in SimpleCov, LCOV, Coveralls, or text formats, and renders summary tables
  directly to GitHub Actions Job Summaries (=$GITHUB_STEP_SUMMARY=).

* Specifying Dependencies
:PROPERTIES:
:CUSTOM_ID: specifying-dependencies
:END:

=elisp-ci= supports three dependency resolution strategies (in order of priority):

1. *Step-Level Input* (=with: dependencies: "..."=): Overrides dependencies for
   a specific step. Accepts space/comma-delimited strings or YAML block scalars.
2. *Workflow or Job-Level Environment Variable* (=env: ELISP_CI_DEPENDENCIES: "..."=):
   Declares dependencies *once* for the entire workflow or job without repeating
   them across lint, test, and coverage steps.
3. *Automatic Header Extraction (Default, Zero-Config)*: If no explicit dependencies
   are given, =elisp-ci= automatically inspects =;; Package-Requires:= in all
   =*.el= files, filters out Emacs built-ins, and installs required packages.

#+begin_src yaml
# Example: Setting dependencies once at the job level
jobs:
  test:
    runs-on: ubuntu-latest
    container: silex/emacs:${{ matrix.emacs_version }}-ci
    env:
      ELISP_CI_DEPENDENCIES: "compat transient"
    steps:
      - uses: actions/checkout@v5
      - uses: tychoish/elisp-ci/lint@main
      - uses: tychoish/elisp-ci@main
      - uses: tychoish/elisp-ci/coverage@main
#+end_src

* Actions
:PROPERTIES:
:CUSTOM_ID: actions
:END:

** Test Runner
:PROPERTIES:
:CUSTOM_ID: test-runner
:END:

Runs ERT or ELPAish test suites with automatic archive, keyring, and dependency bootstrapping.

*Action reference*: =tychoish/elisp-ci@main=

*** Options

| Input                 | Default                                                     | Description                                                  |
|-----------------------+-------------------------------------------------------------+--------------------------------------------------------------|
| =dependencies=        | =""= (auto-discovered)                                      | ELPA packages (or set via =ELISP_CI_DEPENDENCIES=).          |
| =test-files=          | ="test/test-*.el"=                                          | Test files or globs to load and execute (delimited string).   |
| =load-paths=          | ="."=                                                       | Directories to prepend to =load-path= (delimited string).     |
| =archives=            | ="gnu nongnu melpa elpaish"=                                | Enabled package archives (space-delimited string).           |
| =unsigned-archives=   | ="elpaish"=                                                 | Fallback unsigned archive whitelist (space-delimited string).|
| =keyring-url=         | ="https://tychoish.github.io/elpaish/elpaish-keyring.gpg"=   | Remote GPG keyring for ELPAish signature verification.        |
| =runner=              | ="ert"=                                                     | Test runner engine (=ert= or =elpaish=).                     |

*** Basic Configuration

Run test suites using zero-configuration auto-discovery:

#+begin_src yaml
- name: Run Tests
  uses: tychoish/elisp-ci@main
#+end_src

** Linter
:PROPERTIES:
:CUSTOM_ID: linter
:END:

Runs byte-compilation, =checkdoc=, =package-lint=, and =relint= across source files with customizable failure thresholds.

*Action reference*: =tychoish/elisp-ci/lint@main=

*** Options

| Input                 | Default                                                     | Description                                                  |
|-----------------------+-------------------------------------------------------------+--------------------------------------------------------------|
| =dependencies=        | =""= (auto-discovered)                                      | Dependencies (or set via =ELISP_CI_DEPENDENCIES=).           |
| =target-files=        | ="*.el"=                                                    | Source files or globs to analyze (delimited string).          |
| =load-paths=          | ="."=                                                       | Directories to prepend to =load-path= (delimited string).     |
| =linters=             | ="byte-compile checkdoc"=                                   | Active linters (space-delimited string).                      |
| =required-linters=    | ="byte-compile checkdoc"=                                   | Linters that block build on failure (space-delimited string).|
| =fail-on-warning=     | ="true"=                                                    | Treat byte-compiler warnings as fatal errors.                |
| =archives=            | ="gnu nongnu melpa elpaish"=                                | Enabled package archives (space-delimited string).           |
| =unsigned-archives=   | ="elpaish"=                                                 | Fallback unsigned archive whitelist (space-delimited string).|
| =keyring-url=         | ="https://tychoish.github.io/elpaish/elpaish-keyring.gpg"=   | Remote GPG keyring URL.                                      |

*** Basic Configuration

#+begin_src yaml
- name: Lint Source Files
  uses: tychoish/elisp-ci/lint@main
#+end_src

** Code Coverage
:PROPERTIES:
:CUSTOM_ID: coverage
:END:

Instruments source files using =under-cover=, executes test suites, exports
coverage artifacts, and outputs a formatted markdown summary into
=$GITHUB_STEP_SUMMARY=.

*Action reference*: =tychoish/elisp-ci/coverage@main=

*** Options

| Input                 | Default                                                     | Description                                                  |
|-----------------------+-------------------------------------------------------------+--------------------------------------------------------------|
| =dependencies=        | =""= (auto-discovered)                                      | Dependencies (or set via =ELISP_CI_DEPENDENCIES=).           |
| =source-files=        | ="*.el"=                                                    | Source files to instrument for coverage (delimited string).  |
| =test-files=          | ="test/test-*.el"=                                          | ERT test files or globs to run (delimited string).           |
| =load-paths=          | ="."=                                                       | Directories to prepend to =load-path= (delimited string).     |
| =report-format=       | ="simplecov"=                                               | Format: =simplecov=, =lcov=, =coveralls=, or =text=.         |
| =output-file=         | ="coverage.json"=                                           | Target file path for generated coverage data.                |
| =archives=            | ="gnu nongnu melpa elpaish"=                                | Enabled package archives (space-delimited string).           |
| =unsigned-archives=   | ="elpaish"=                                                 | Fallback unsigned archive whitelist (space-delimited string).|
| =keyring-url=         | ="https://tychoish.github.io/elpaish/elpaish-keyring.gpg"=   | Remote GPG keyring URL.                                      |

*** Codecov Integration Workflow

#+begin_src yaml
- name: Measure Test Coverage (LCOV)
  uses: tychoish/elisp-ci/coverage@main
  with:
    report-format: "lcov"
    output-file: "coverage/lcov.info"

- name: Upload Coverage to Codecov
  uses: codecov/codecov-action@v5
  if: ${{ !cancelled() }}
  with:
    files: coverage/lcov.info
    fail_ci_if_error: false
#+end_src

* Supported Linters & Quality Gates
:PROPERTIES:
:CUSTOM_ID: supported-linters
:END:

The =lint= sub-action supports four integrated analyzers:

1. =byte-compile=: Runs Emacs batch byte-compilation. When =fail-on-warning: true=
   is set, compiler warnings (unresolved variables, obsolete functions,
   missing requires) fail the build.

2. =checkdoc=: Validates standard Emacs Lisp docstring style (sentence casing,
   capitalization of parameter names, header comments).

3. =package-lint=: Validates compliance with ELPA/MELPA packaging standards
   (automatically installed if enabled).

4. =relint=: Scans regular expressions for syntax errors and inefficiencies
   (automatically installed if enabled).

* Example Workflow
:PROPERTIES:
:CUSTOM_ID: example-workflow
:END:

A complete, zero-boilerplate CI matrix workflow with linting, ERT tests, code coverage, and Codecov upload:

#+begin_src yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    container: silex/emacs:${{ matrix.emacs_version }}-ci
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        emacs_version:
          - "29.4"
          - "30.2"

    steps:
      - uses: actions/checkout@v5

      - name: Lint Source Code
        uses: tychoish/elisp-ci/lint@main

      - name: Run ERT Tests
        uses: tychoish/elisp-ci@main

      - name: Measure Code Coverage
        uses: tychoish/elisp-ci/coverage@main
        with:
          report-format: "lcov"
          output-file: "coverage/lcov.info"

      - name: Upload Coverage to Codecov
        uses: codecov/codecov-action@v5
        if: ${{ !cancelled() }}
        with:
          files: coverage/lcov.info
          fail_ci_if_error: false
#+end_src

* License
:PROPERTIES:
:CUSTOM_ID: license
:END:

GPL-3.0-or-later. See [[file:LICENSE][LICENSE]] for details.
